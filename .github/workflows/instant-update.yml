name: Instant Update on Push

on:
  push:
    branches: [ main ]
    paths:
      - 'docs/products.json'

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Trigger GitHub Pages rebuild
        run: echo "✅ Instant update triggered!"
